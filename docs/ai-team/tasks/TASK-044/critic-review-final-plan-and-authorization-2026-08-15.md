# TASK-044 — Critic Review, Final Plan and Authorization

## Critic cycle 1

1. `CRITICAL / CLOSED`: a generic Timeline could become a second Edit/Audio truth.
   It is a projection plus versioned composition; TASK-007 and TASK-042 remain
   source owners and every semantic application hash-binds them.
2. `CRITICAL / CLOSED`: Export enqueue could be mistaken for external execution
   authority. Durable QUEUED planning grants no Resolve/render authority; exact
   one-shot prepare/apply remains mandatory immediately before dispatch.
3. `HIGH / CLOSED`: clip click and Candidate click could collide. Generic clips
   select only; Cut Candidate retains explicit review selection; seek is separate.
4. `HIGH / CLOSED`: CSS/microsecond drift could contradict Blueprint frames. One
   rational frame transform drives every viewport projection.
5. `HIGH / CLOSED`: session zoom/selection could pollute Product Undo/Autosave.
   Presentation state is separate from semantic edit history.
6. `HIGH / CLOSED`: Execute All could create blanket authority. It iterates exact
   per-job confirmations and stops/parks independently.

## Critic cycle 2

1. `HIGH / CLOSED`: output paths could leak into durable/public records. Only a
   logical output target persists; the host path remains launcher-private.
2. `HIGH / CLOSED`: restart might replay an external render. DISPATCHING/RUNNING
   reopens as UNKNOWN with no automatic transition to READY.
3. `HIGH / CLOSED`: remove-track could orphan required clips. Prepare validates
   minimum roles and dependency emptiness; apply is Human-final authority.
4. `HIGH / CLOSED`: STRETCH/crossfade could be silently degraded. Unsupported
   adapter features remain explicit gaps and block execution compilation.
5. `HIGH / CLOSED`: 10,000-item testing could claim unmeasured native performance.
   deterministic window/DOM bounds are automated; exact latency budgets and native
   measurements remain Evidence gates.
6. `MEDIUM / CLOSED`: released static projections could break. They remain readable
   and are adapted into the new read model; no in-place schema reinterpretation.

Unresolved Critical/High: `0 / 0`.

## Final implementation order

1. Host this zero-code current-main audit/design and complete branch cleanup.
2. Fresh-main AUTONOMY selects `BVP-TASK-044-P-NLE-1 / IMPLEMENTATION`.
3. Implement semantic projection/reducer/windowing and Shell specs; test, Critic,
   PR, hosted merge and cleanup.
4. Repeat fresh-main flow for P-NLE-2 semantic editing/history.
5. Repeat for P-NLE-3 durable Export Queue composition.
6. Repeat for P-NLE-4 existing Shell/UI and native interaction acceptance.
7. Synchronize exact hosted closure and hand off to TASK-045 release decision.

## Judge decision

- Design unit: `AUTHORIZED`
- P-NLE-1 implementation: `AUTHORIZED_AFTER_DESIGN_HOSTED_CLOSURE_AND_FRESH_MAIN_RESELECTION`
- P-NLE-2..4: `DEPENDENCY_WAIT` until the preceding unit is hosted-closed and
  re-audited from fresh main
- Provider/paid/new credential/Production Deploy: `NOT_AUTHORIZED`
- native operations: only P-NLE-4 sandboxed acceptance under existing authority
- version/Tag/GitHub Release: `NOT_TASK_044 / TASK_045_DECISION`

