# TASK-047 terminal writer/completeness R0 candidate checkpoint

- Project / Task / Unit: BAI VIDEO PRODUCTION / TASK-047 / `P-OBS-1B-TERMINAL-WRITER-COMPLETENESS-R0-DESIGN`; `main@89c38719cc2158eb26d0e3f48cd1e0a0ba645c32`; DEV-4 design candidate.
- Allowed paths: the adjacent candidate design and this bounded Evidence note only. No source/schema/test, other Task contract, native/OBS/private-audio, custody/Asset, Release, Deploy or Production effect.
- Exact-source review: `BaiCaptureOperation.cs` lines 118-218 show lease fencing, 2-second settlement and unbounded fallback; lines 254-303 show sink lock/write/flush/dispose. `BaiVoiceCaptureController.cs` lines 1730-1799 show UI stop, fallback, float-WAVE finalization, partial-to-final move and legacy receipt. `test_task047_readiness_monitor_source_contract.py` deliberately asserts this legacy sequence and would need separate migration if code changes. R0 schema lines 214-343 contain the transport state and counts but no terminal writer/completeness state.
- Candidate decision: distinguish capture completeness, writer/readback state, accepted-to-written coverage and R0 transport claim. Closed/read-back *partial* raw material remains recovery-only. A matching readback of shortened output is not whole-take proof. Mismatched/unknown transport cannot support capture completeness; an unclosed writer can coexist with complete capture but never composite success or publication. Existing R0 and TASK-082 authority remain unchanged.
- Independent DEV-4 Critic (read-only) found `0 Critical / 2 High / 2 Medium`: R0 transport requires at least one observed packet, so a packetless abort cannot have an R1 terminal predecessor; readback match does not prove all accepted frames were written; current source/classification and R0 error-counter semantics need explicit separation/checks. Independent Tester statically accepted the safety separation but failed the COMPLETE success row with a two-packet/half-written-object counterexample; native/pytest evidence remains `NOT_CONFIRMED`.
- Candidate revision addresses those findings as **design proof obligations**, not as an ABI freeze: packetless failure routes to an unallocated versioned attempt-failure/recovery record outside R1; `writer_coverage_state=EXACT` needs durable ordered, exactly-once accepted→append→encoded-byte accounting plus actual-object readback; source eligibility is separate from capture continuity; future R1 producer/triple validator must reject contradictory R0 loss/reorder/overrun claims. The current producer cannot assert `EXACT`. The independent Tester rereview found the half-written-object counterexample blocked at design level (`PASS`), with implementation/native proof `NOT_CONFIRMED`. The independent Critic rereview found a wording conflict around `COMPLETE` capture plus writer failure; the final correction preserves this combination as failure/recovery-only in the private journal and gates any public terminal on the unallocated failure ABI. Final narrow Critic confirmation reports `0 Critical / 0 High / 0 Medium / 0 Low` for the revised design candidate. Exact field/nullability, ledger/readback issuers and public/private split remain open; no technical runtime PASS is claimed.
- Design-only Judge decision: `ACCEPT_GAP_RESOLUTION_CANDIDATE`, limited to the four-dimensional safety partition and explicit proof obligations above. This does **not** accept/freeze an R1 terminal ABI, allocate any producer or failure-record owner, authorize schema/parser/native changes, or unblock TASK-098 A5. The unresolved producer, time, byte-layout, nullability and custody boundaries remain gates.
- Verification executed: targeted source/schema/contract read and candidate diff/Markdown-link check only; local unit/integration/native tests `NOT_EXECUTED / NOT_CONFIRMED`. No build, QA, installer, runtime, fixture, private media or temporary test output root was created.
- Current gates and next: trusted-time producer owner, numeric `B/G`, independent deadline supervisor/journal, source-frame origin/accounting, packetless failure record, failure-terminal nullability and rest of R1 matrix unbound. Persist this reviewed design candidate; a separate exact owner/ABI decision is required before code/schema/native work. TASK-098 A5 remains blocked.
