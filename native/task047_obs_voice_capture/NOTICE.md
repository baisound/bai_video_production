# Notices

- Module: `bai-voice-capture`
- Development version: `0.1.0-dev.1`
- License expression: `GPL-2.0-or-later`
- Intended host baseline: OBS Studio 32.2.1 x64 on Windows.
- OBS Studio is a separate upstream work licensed under GPL-2.0-or-later.
- Windows CNG (`bcrypt`) is used for local message authentication.

This package contains no OBS binary, Qt binary, dependency archive, audio,
credential, device identifier, recording, dataset, or trained model. A
runtime ZIP may be produced only from the verified offline dependency tree
and must carry the actual dependency manifest generated at build time.
