# TASK-073 P0-V Owner Voice Local WAV — Complete Design Packet D2

## 1. Design identity

- Task: `TASK-073`
- Atomic responsibility: `OWNER_VOICE_LOCAL_WAV_PRODUCT_COMPOSITION_V2`
- Design revision: `D2`
- Reconciliation base: `origin/main@70ba9e369887d3d7ded59e7197d20d133b2b4d38`
- Profile: `DEV-4 FOUNDATION CRITICAL`
- State: `REVIEW_ACTIVE / SUCCESSOR_MOCK_OWNER_CHECK_PENDING / SOURCE_START0`
- Product entry: unified `BAI Video Production.exe`
- Primary route: installed local/free voice generation
- Default compute preference: `AUTO`
- Cloud/paid fallback: absent

D1 remains unchanged in
`p0v-owner-voice-local-wav-complete-design.md`.  Its exact hashes and failed
review are recorded in `design-review-receipt.md`.  D2 is a bounded correction,
not a claim that any producer effect already exists.

## 2. Owner outcome and two completion results

The Owner outcome is:

```text
normal EXE
→ Voice Studio
→ current local/free Voice route selection
→ consented Owner reference
→ admitted local inference
→ technical QA
→ Owner listening decision
→ accepted 48 kHz / 24-bit integer PCM / mono WAV
```

`TASK073_IMPLEMENTATION_COMPLETE` and `P0V_OWNER_OUTCOME_VERIFIED` are
different results.

- `TASK073_IMPLEMENTATION_COMPLETE` requires the accepted design, composition
  implementation, all focused/negative/regression tests, packaged synthetic
  E2E and independent DEV-4 closure.
- `P0V_OWNER_OUTCOME_VERIFIED` additionally requires one real, private,
  Owner-authorized E3-E5 run whose reference, runtime, WAV, QA and listening
  receipts are all exact-current `PASS`.
- Synthetic evidence or `NOT_CONFIRMED` never produces the Owner outcome.

"Optimal" means the Owner-selected candidate among currently valid candidates
after explicit technical measurements.  It is not an objective universal-best
claim and is never inferred from a model label or scalar score alone.

## 3. D1 review closure map

| D1 finding | D2 correction |
|---|---|
| Second canonical Quick Clone state machine | TASK-073 owns only derived composition/projection; TASK-046 lifecycle remains sole authority. |
| Direct TASK-070 consumption | Only TASK-070 → TASK-063 → TASK-072 → P0-E installed context is admitted. |
| Missing Voice action/profile ABI | TASK-074 owns exact TASK-071/TASK-072 amendments defined in section 7. |
| Durable recovery collision | TASK-076 owns the limited TASK-020/043 secure amendment; TASK-073 projects its receipts. |
| Voice compute route disabled | TASK-075 owns actual `audio.voice.local` runtime mapping and live admission. |
| No canonical Voice route selection store | TASK-074 extends the TASK-046 Voice domain with one CAS/readback route-selection revision. |
| No enforced offline boundary | TASK-075 owns the native offline child boundary and receipt. |
| No safe Owner playback port | TASK-075 owns private pinned playback and exact child cleanup. |
| Task completion confused with real result | Section 2 defines separate results. |
| V2 named packaged before package/UI | Sequence now separates application fixture from installed packaged E2E. |
| Raw reference lifecycle incomplete | TASK-074 owns exact private import/retention/revocation lifecycle. |

## 4. Canonical responsibility map

| Fact/effect | Sole producer/owner | TASK-073 use |
|---|---|---|
| VoiceProfile, Consent, private reference, Dataset, Quick Clone revision, selected Voice route | TASK-046 plus TASK-074 limited amendment | Read verified projection only. |
| Narration plan, synthesis semantics, output sink and WAV result/publication | TASK-014 limited amendment; TASK-075 consumes its callable/sink | Request and correlate opaque receipt. |
| Technical voice QA | TASK-048 plus TASK-075 binding | Display exact facts; never mint PASS. |
| Installed model inventory | TASK-013 / TASK-046 route projection | Display currently admitted public-safe entries. |
| CPU/GPU/AUTO preference and live compute | TASK-066 plus TASK-075 Voice runtime mapping | Display preference/effective backend separately. |
| Human action/challenge | TASK-071 plus TASK-074 amendment | Request plan; display broker state. |
| One-shot ticket/config/child identity | TASK-072 plus TASK-074/TASK-075 consumer profiles | Correlate opaque operation id only. |
| Install roots/descriptor/owner | TASK-070 → TASK-063 → TASK-072 → P0-E | Consume only `INSTALLED_STARTUP_CONTEXT_V1`. |
| Durable Product Job/recovery | TASK-020/TASK-043 plus TASK-076 correction | Display durable job terminal/currentness. |
| Secure artifact I/O | TASK-068 and each producer | No direct call from TASK-073. |
| Playback and owned playback child | TASK-075 | Display play/stop/error state. |
| Shell, global single instance, packaged launcher | TASK-036 P0-E | Limited UI/entry amendment after its terminal handoff. |
| Asset adoption / Timeline / Export | TASK-003 / TASK-044 | Separate next action only; effect zero here. |

## 5. TASK-073 responsibility boundary

### 5.1 TASK-073 owns

- a closed, non-authoritative composition document;
- request identity and opaque producer-receipt correlation;
- freshness/mismatch evaluation over already verified public-safe projections;
- a Voice Studio view model and actionable Japanese copy;
- application-level synthetic fixtures;
- packaged synthetic E2E after all package/UI gates;
- separate display of implementation completion and real Owner outcome.

### 5.2 TASK-073 never owns

- a canonical operation transition or durable state machine;
- filesystem open/create/write/replace/delete/cleanup;
- authority mint, deserialize, consume or burn;
- model/runtime/GPU load, child launch/kill, network policy or playback;
- reference preprocessing, training, synthesis, WAV publication or QA;
- recovery/repair of Project, Job, Voice, install or output state;
- a second selector, VoiceProfile, Job, Asset or result store.

TASK-073 sends a versioned request to the responsible Product port and later
reads the producer's public-safe typed projection.  It never forwards a raw
path, hash, revision, token, backend, clock, PID or capability through UI/argv.

## 6. Producer Tasks allocated by D2

### 6.1 TASK-074 — Owner Voice Authority, Selection and Private Reference

Profile: `DEV-4`。Design/implementation line: Design A with Design B review;
Outcome V primary, with the Voice trust worker reviewing authority/I/O.

It owns one bounded cross-owner amendment that:

- adds exact Voice action codes and TASK-072 consumer profiles;
- adds a TASK-046-owned `VoiceProfileRouteSelection` CAS/readback API;
- binds route selection to current VoiceProfile, Project, installed route,
  license and source mode without loading or executing the model;
- owns private reference import/in-place classification, encrypted derivative,
  private root/DACL, retention, revocation and exact-owned cleanup policy;
- returns public-safe projections and private in-process producer receipts;
- does not dispatch training, inference, playback, Asset or Export.

The durable selection API is gated by the TASK-036 P0-E owner correction that
provides secure canonical Project/SQLite bootstrap and CAS readback.  Before
that exact completion receipt, the UI may choose a route only inside one
operation plan; it must show `保存済み` as false and must not create a new
path-based settings file.  Physical purge is a separate Human-gated,
handle-bound TASK-074 lifecycle subphase; TASK-068 immutable publication alone
does not authorize delete.

### 6.2 TASK-075 — Local Voice Execution and Listening Broker

Profile: `DEV-4`。Design B platform lead with Design A Product review;
Outcome V implementation and Outcome E native/package QA.

It owns one bounded local execution broker that:

- maps `audio.voice.local` to an actual supported CPU/GPU/AUTO runtime route;
- fixes native backend/probe/runtime identity inside one operation;
- enforces a versioned offline execution boundary with public-safe readback;
- consumes TASK-014's `LOCAL_PRIMARY_NARRATION_CALL_PROFILE_V2` and
  `NARRATION_OUTPUT_SINK_CAPABILITY_V1` without taking narration semantics or
  publication ownership;
- uses TASK-072's one-shot child identity/profile and TASK-076 durable job;
- returns an exact local synthesis result receipt;
- binds TASK-048 technical QA;
- provides pinned private play/stop/close with exact owned-child cleanup and
  path-free diagnostics;
- never performs automatic retry, model/backend fallback, training, Asset or
  Export.

### 6.3 TASK-076 — Durable Product Job Secure Artifact Corrective

Profile: `DEV-4`。Design B and the Outcome V trust worker.

It is a limited TASK-020/TASK-043 cross-owner amendment.  TASK-068
`IMMUTABLE_ONLY_V1` cannot safely replace a mutable `jobs.json`; therefore
TASK-076 uses append-only immutable job generations bound to an exact
predecessor.  Current-head selection comes only from a TASK-043-owned sealed
`PROJECT_CHILD_CURRENTNESS_RECEIPT_V2` after the canonical Project manifest
currentness correction.  Directory scan, highest/mtime/first-absent selection,
mutable replace and caller-selected head are prohibited.  TASK-076 returns
typed job/current-child receipts, is reusable by P0-V/P0-E/P0-L and owns no
Voice semantics.

TASK-074/075/076 are parallel design units on two design lines.  Their
implementation feeds Outcome V and then hands only versioned receipts to the
single TASK-036 integration owner in Outcome E.  They do not create additional
permanent lines or interrupt an already in-flight Atomic Unit.

## 7. Versioned producer ABIs

### 7.1 Installed startup context

The only admitted installed coordinate is:

```text
TASK-070 private pair
→ TASK-063 INSTALLATION_READBACK_V2
→ TASK-072 INSTALLED_INSTANCE_PROFILE_BINDING_V1
→ TASK-036 P0-E INSTALLED_STARTUP_CONTEXT_V1
→ TASK-073 public-safe readiness projection
```

TASK-073 cannot import TASK-070 types, parse descriptor/owner files or accept an
install/bridge/model root from caller text.

Before real producers are canonical, TASK-073 may consume
`P0E_PROJECT_BOOTSTRAP_FIXTURE_V1` and private
`INSTALLED_STARTUP_CONTEXT_FIXTURE_V1` only as pure test inputs.  Both must be
marked `fixture_only=true`, `authority_created=false` and
`production_eligible=false`.  Copy, serialization, replay, wrong
instance/build/session/consumer or stale fixture is effect zero.  Real binding
requires a terminal `PROJECT_CANONICAL_SECURE_BOOTSTRAP_V1` private readback
and fresh `INSTALLED_STARTUP_CONTEXT_V1`; a fixture or public mapping can never
mint Product authority.

### 7.2 TASK-071 Voice action amendment

TASK-074 adds these closed Human actions through `HUMAN_ACTION_REGISTRY_V2`
after TASK-071 base implementation is canonical and overlap-free:

- `OWNER_VOICE_REFERENCE_PREPARE_V1`
- `OWNER_VOICE_LOCAL_INFERENCE_V1`
- `OWNER_VOICE_LISTENING_DECISION_V1`
- `OWNER_VOICE_REGENERATE_V1`
- `OWNER_VOICE_REFERENCE_REVOKE_V1`
- `OWNER_VOICE_REFERENCE_PURGE_V1`

Each action binds exact Project/install/session, VoiceProfile, operation plan,
expected producer revision/head, expiry and trusted Human event.  IDs, time,
decision and challenge cannot be caller-selected.  Accept/reject/retest are
values inside the TASK-041-owned listening decision; they do not become three
parallel state authorities.  Route selection and play/stop are explicit UI
operations but are not Human-action receipts.

### 7.3 TASK-072 Voice consumer profiles

TASK-074/TASK-075 add these closed profiles through `ACTION_REGISTRY_V2`:

- `task074.owner_voice.reference.prepare`
- `task074.owner_voice.profile_route.select`
- `task075.owner_voice.local.inference`
- `task075.owner_voice.private.playback`
- `task041.owner_voice.listening.decision`
- `task014.owner_voice.regenerate`
- `task074.owner_voice.reference.revoke`
- `task074.owner_voice.reference.purge`

Profiles bind action, Product build, installed instance, operation config,
invocation budget `1`, expiry, exact child/IPC where applicable and terminal
consume/revoke state.  Copy, serialization, wrong subcommand, second use,
concurrent use and exception reuse are effect zero.

### 7.4 Voice route selection

`VOICE_PROFILE_ROUTE_SELECTION_V1` belongs to TASK-046 through TASK-074.  It
stores no credentials, path or authority capability.  CAS/readback binds:

- Project and VoiceProfile revision;
- `ZERO_SHOT_LOCAL` or `FINE_TUNED_LOCAL`;
- public route key and installed-route binding digest;
- required reference or ModelCandidate source mode;
- CPU/GPU/AUTO preference reference, not an effective backend claim;
- revision, predecessor and canonical self-hash.

Saving selection never downloads, loads, trains, probes or infers.  Durable
save is unavailable until the P0-E secure canonical Project/SQLite bootstrap
receipt is current; before that, an exact one-operation selection is allowed
without claiming persistence.  A route is runnable only after TASK-075
resolves fresh live runtime/compute admission.

## 8. One-direction dependency graph

```text
TASK-068 ───────────────→ TASK-076 durable job correction

P0-E secure Project/SQLite bootstrap → TASK-074 durable selection

TASK-070 → TASK-063 → TASK-072 installed binding → TASK-036 P0-E context

TASK-071 base + TASK-072 base + TASK-046/TASK-013
                         → TASK-074 authority/selection/private reference

PR #470 + PR #476 + TASK-014 PRE + TASK-066
+ TASK-071 + TASK-072 + TASK-074 + TASK-076
                         → TASK-075 native inference
                         → TASK-014 POST WAV publication/alignment
                         → TASK-048 technical QA
                         → TASK-075 private playback
                         → TASK-071 + TASK-041 listening decision
                         → TASK-075 QA/listening join receipt

P0-E context + TASK-074 + TASK-075 + TASK-076
                         → TASK-073 composition/fixture receipt
                         → Outcome E TASK-036 UI/package integration

TASK-073 implementation complete + explicit Owner native Gate
                         → real E3-E5 PASS
                         → P0V_OWNER_OUTCOME_VERIFIED
```

No arrow points from TASK-073 back into a producer.  Missing receipts park only
the affected effect; contract, UI, fixture and negative work continue.

## 9. Derived UI projection, not a state authority

TASK-073 may display these derived labels, but stores none of them:

| UI label | Sole evidence |
|---|---|
| `SETUP_REQUIRED` | Missing P0-E installed context or producer readiness. |
| `REFERENCE_REQUIRED` | TASK-046 Quick Clone/readiness projection. |
| `MODEL_SELECTION_REQUIRED` | No current TASK-074 selection revision. |
| `READY_TO_PREPARE` | Current Consent/profile/selection, no reference receipt. |
| `READY_TO_RENDER` | TASK-074 reference + TASK-075 runtime readiness. |
| `CONFIRMATION_REQUIRED` | TASK-071 plan current, no consumed ticket. |
| `QUEUED` / `RUNNING` | TASK-076 durable job projection. |
| `RECOVERY_REQUIRED` / `UNKNOWN` | TASK-076/TASK-075 typed terminal state. |
| `QA_REQUIRED` | TASK-075 result current, TASK-048 QA absent. |
| `LISTENING_REQUIRED` | Current QA PASS, no TASK-071 listening decision. |
| `WAV_ACCEPTED` / `WAV_REJECTED` | Exact TASK-075 QA/listening join plus TASK-041 decision bound to the TASK-071 Human receipt. |

Contradictory, missing, stale or multiple projections produce `BLOCKED` with a
stable reason code.  TASK-073 never chooses a winner, repairs state, advances a
job or retries an operation.

## 10. Product UI and successor mock

The successor Voice Studio mock is a required canonical design gate under
TASK-046 P-VS-1B.  It preserves V6.1.1 rather than redesigning the Product.

### 10.1 Visual plan

- palette: `#0a0d11` background, `#090c10` chrome, `#12171d` panel,
  `#171e26` raised surface, `#2a333d` line, `#edf2f6` text,
  `#8e9aa6` muted, `#4d8fd1` active, `#59c792` verified,
  `#dfa654` Human/wait, `#dd6868` blocked;
- typography: existing `Segoe UI`, `Noto Sans JP`, `Yu Gothic UI`, no new web
  font or branding system;
- layout: existing chrome/stage bar, three-column dense desktop workspace;
- signature element: a receipt-backed `Voice Signal Rail` showing
  `参照 → モデル → 生成 → QA → 試聴` with actual source state, not a decorative
  progress stepper;
- responsive behavior: preserve usable ordering and no horizontal control loss
  at the existing supported desktop widths.

### 10.2 User flow

1. `モデル` — installed local/free route and CPU/GPU/AUTO preference;
2. `参照音声` — current Consent and private-reference state;
3. `原稿` — TASK-014 approved script/style facts;
4. `事前確認` — model/runtime/license/compute/privacy/output facts;
5. `生成確認` — trusted Human operation request;
6. `進行状況` — producer-owned job state and recovery guidance;
7. `試聴・品質` — measured facts, play/stop and Human decision;
8. `WAV` — accepted private staging readback and separately gated next actions.

The UI never asks for an internal path, model hash, revision, store ID, PID,
backend token or authority receipt.  Errors name the failed fact and next safe
action in Japanese without echoing private values.

### 10.3 Owner check gate

The HTML mock and manifest are `OWNER_CHECK_PENDING`.  No TASK-036 source may
change until the Owner checks the complete flow and the manifest records the
exact accepted mock hash/revision.  An Owner statement that the general idea is
good does not substitute for checking the current exact artifact.

Keyboard focus is visible, controls have accessible labels, state is not color
only, motion is optional and disabled by `prefers-reduced-motion`.

## 11. Producer-side effect rules

TASK-073 describes required facts but does not execute them.

### Reference preparation — TASK-074

- trusted picker/registry resolves an allowed coordinate; no caller raw path;
- pinned no-follow read, strict bounds, current Consent and private DACL;
- raw reference remains immutable; derived encrypted material is distinct;
- retention/revocation is explicit and cleanup is exact-owned only;
- preparation never adopts Dataset or starts training.

### Local inference — TASK-075

- one inference action, one ticket, one durable job, one exact child and one
  TASK-014-owned output sink;
- backend fixed from live Task066 Voice admission; no silent fallback;
- offline/native boundary and local-files-only runtime verified by receipt;
- no automatic retry, model reselection or backend switch;
- exception burns the operation; restart resolves authoritative state anew.

### WAV and listening — TASK-014/TASK-048/TASK-075/TASK-041

- exact `48000 Hz`, mono, signed 24-bit integer PCM;
- identity-bound publication through TASK-068 primitives and TASK-014 owner;
- clipping, silence/dropout, duration, level and boundary/style facts remain
  separate; unsupported dimensions remain `UNKNOWN`;
- playback receives a pinned candidate capability, not a path, and owns exact
  play/stop/close child identity;
- TASK-041 owns accept/reject/retest semantics; TASK-071 supplies the exact
  Human decision receipt, while regenerate is a separate action;
- acceptance retains private staging only; Asset/Timeline/Export effect zero.

## 12. Packaged verification

### 12.1 Application-level synthetic fixture

Before UI/package amendments, TASK-073 tests composition using public-safe
typed synthetic producer projections.  It performs filesystem/model/audio/
child effects zero and proves mismatch/currentness/privacy behavior.

### 12.2 Installed packaged synthetic E2E

After the mock Owner check, GF-B/P0-E canonical merge/lock release, producer
completion and fresh overlap audit, the installed EXE uses an isolated test
profile and deterministic non-Owner fixture.  It proves:

- normal launch and one visible Product window;
- losing second launch has voice/private/model effect zero;
- selection CAS/readback without model load/download;
- Ollama absence cannot block Voice;
- exact UI route reaches deterministic canonical-format WAV readback;
- displayed state matches producer receipts;
- bounded rotated logs contain no body or private path.

Synthetic PASS creates no Owner-audio or Production authority.

### 12.3 Real Owner E3-E5

A separate explicit native Human Gate selects a private test Project and one
consented Owner reference.  Result, QA and listening receipts remain private;
only body-free status is public.  `NOT_EXECUTED` is reported as
`NOT_CONFIRMED`, never PASS.

## 13. Acceptance matrix

| ID | Acceptance |
|---|---|
| A01 | TASK-073 has no canonical state transition/store and no effect authority. |
| A02 | Every UI state maps to one named current producer receipt. |
| A03 | TASK-070 is reachable only through TASK-063→TASK-072→P0-E context. |
| A04 | TASK-074 closes all Voice action/profile and private-reference authority. |
| A05 | TASK-075 closes actual Voice compute, offline execution, inference and playback. |
| A06 | TASK-076 uses immutable generations plus sealed Project currentness before real execution. |
| A07 | Route selection is TASK-046-owned CAS/readback and causes no runtime effect. |
| A08 | Local/free catalog contains only current installed/licensed/admitted entries. |
| A09 | CPU/GPU/AUTO display preference and effective backend separately. |
| A10 | GPU failure never silently falls back; CPU never initializes CUDA. |
| A11 | Ollama state neither blocks nor authorizes Voice. |
| A12 | Reference preparation is private, consented, bounded and training-free. |
| A13 | One exact Human action maps to one exact Task072 consumer/ticket. |
| A14 | Runtime is local-only, offline, one child/one output and non-retrying. |
| A15 | Durable job and child identity survive/reconcile crash without Task073 repair. |
| A16 | WAV format/publication and TASK048 QA are exact-current. |
| A17 | Playback is pinned, path-free publicly and exact-child controlled. |
| A18 | TASK-041 accept/reject/retest values and the regenerate action cannot substitute for one another or replay. |
| A19 | Voice Studio successor mock preserves V6.1.1 and has exact Owner check. |
| A20 | UI is keyboard operable and shows actionable Japanese states. |
| A21 | Packaged synthetic E2E uses the normal EXE/global single instance. |
| A22 | Public UI/log/receipt contains no raw audio/text/path/Voice ID/secret. |
| A23 | Implementation complete and real Owner outcome are reported separately. |
| A24 | WAV acceptance does not imply Dataset, training, Asset, placement or Export. |
| A25 | One coherent TASK-073 PR begins only after its entire design scope is accepted. |

## 14. Negative matrix

| ID | Condition | Required result |
|---|---|---|
| N01 | TASK-073 attempts transition/persist/mint/burn | Test/design FAIL; effect zero. |
| N02 | Direct TASK-070/private descriptor access | Closed reject. |
| N03 | Missing/stale/multiple P0-E startup context | Voice blocked; private read zero. |
| N04 | Unknown action/consumer key or cross-action ticket | Effect zero. |
| N05 | Copied/rehashed/deserialized receipt/capability | Authority zero. |
| N05a | Mutable jobs.json replace or directory-scan current-head selection | Test/design FAIL; job authority zero. |
| N06 | Route save attempts download/load/train/probe | Effect zero and test FAIL. |
| N07 | Unknown/unlicensed/uninstalled/hash-drifted route | Runnable false. |
| N08 | GPU without live admission or CPU/GPU silent switch | No success receipt. |
| N09 | Ollama missing/running/tampered | Voice projection/effect unchanged. |
| N10 | Raw path/UNC/reparse/hardlink/ancestor or inode swap | Producer effect zero. |
| N11 | Revoked/expired/wrong-purpose Consent | Private read/model load zero. |
| N12 | Training starts from reference preparation | Hard fail. |
| N13 | Runtime lacks offline native receipt | Child launch zero. |
| N14 | Second/concurrent ticket or Product instance | Single winner; loser effect zero. |
| N15 | Config/model/reference/backend changes after plan | Old operation burned; no retry. |
| N16 | Job/PID/output identity ambiguous | `UNKNOWN`; Task073 repair/kill zero. |
| N17 | Playback receives path or leaks diagnostic path | Test FAIL; playback zero. |
| N18 | Playback process survives exact stop/app-close receipt | Native QA FAIL. |
| N19 | QA missing/stale/forged/UNKNOWN | Accept action unavailable. |
| N20 | TASK-041 decision or regenerate action reused for the other operation | Effect zero. |
| N21 | UI value differs from fresh canonical projection | Success display zero. |
| N22 | Mock unreviewed or producer branch/lock still active | TASK036 mutation zero. |
| N23 | Synthetic marker removed/copied to Owner profile | Owner outcome authority zero. |
| N24 | Public error/log contains private body/path/token/account | Test FAIL. |
| N25 | Accepted WAV auto-adopts Asset/Timeline/Export | Test FAIL. |

## 15. Fault matrix

| ID | Producer seam | TASK-073 projection/result |
|---|---|---|
| F01 | Human plan before ticket | `CONFIRMATION_REQUIRED`; process zero. |
| F02 | Ticket before durable job | `RECOVERY_REQUIRED`; producer reconciles. |
| F03 | Durable job before child | `QUEUED/RESUME_REQUIRED`; no auto-spawn by Task073. |
| F04 | Child start before load receipt | `UNKNOWN`; old operation unusable. |
| F05 | Backend admission lost before inference | blocked/unknown; no fallback. |
| F06 | Waveform before owned temp | no WAV receipt. |
| F07 | Publish before pinned readback | `UNKNOWN`; QA/accept unavailable. |
| F08 | Result before QA | `QA_REQUIRED`. |
| F09 | QA before listening | `LISTENING_REQUIRED`. |
| F10 | Playback child starts before controller receipt | `UNKNOWN`; accept unavailable. |
| F11 | Listening receipt before producer terminal readback | blocked mismatch. |
| F12 | Acceptance before Asset adoption | accepted staging only. |
| F13 | App/Windows closes mid-run | producer-owned bounded recovery; Task073 no kill/repair. |
| F14 | UI projection read races producer advance | stale projection discarded; fresh read only. |

## 16. Implementation and integration sequence

1. Accept D2 and the successor mock; preserve D1 failed history.
2. Complete TASK-074/075/076 designs in parallel on Design A/B.
3. Implement TASK-073-owned composition/schema/projection files only; all
   ports remain no-effect until producer receipts exist.
4. Complete producer implementations on Outcomes V/L/E without interrupting
   current Atomic Units.
5. Rebind TASK-073 to exact canonical producer completion receipts.
6. Outcome E, as the sole TASK-036 integration owner, creates a separate
   fresh-main TASK-036 UI/package amendment only after GF-B/P0-E implementation
   PRs are merged, locks are released and the exact mock is Owner-checked.
7. After P0-E installed context and producer terminal handoff, Outcome E adds
   packaged entry composition; TASK-073 source remains unchanged.
8. Run the TASK-073 application fixture and its full negative/regression set.
9. Freeze one coherent TASK-073 implementation head and create one Draft PR.
10. After that receipt is canonical, Outcome E runs the installed packaged
    synthetic E2E as part of its separate TASK-036 integration.
11. Run real Owner E3-E5 only through the separate explicit native Gate.

## 17. Verification and evidence

Required before commit-ready:

- schema/static/compile checks;
- focused composition/application/projection tests;
- all acceptance, negative and fault cases;
- TASK-014/046/048/066/071/072/076 focused regressions and the TASK-036
  fixture-consumer contract without changing TASK-036 source;
- independent Critic and Tester;
- Judge PASS with unresolved `Critical/High = 0/0`;
- changed-files/Allowed-Files and path/secret leakage audit;
- hosted Windows/Ubuntu Python 3.11–3.13;
- a Task073 application-level synthetic receipt.

The separate P0-V outcome additionally requires Outcome E's installed packaged
synthetic UI/single-instance/readback evidence and the real Owner evidence
reported separately as `PASS/FAIL/NOT_CONFIRMED`.

## 18. Prohibitions

- no paid/cloud route, fallback or model download;
- no Owner audio, transcript, embedding or model weight in Git/public evidence;
- no training dispatch, Dataset adoption or ModelCandidate approval;
- no raw caller path/output/backend/clock/security provider/fault hook;
- no TASK-073 file/process/capability/recovery effect;
- no direct TASK-070 private receipt consumption;
- no second VoiceProfile, selection, Job, Asset or result store;
- no automatic retry, backend/model switch or unknown-state repair;
- no TASK-036 mutation by TASK-073; Outcome E owns the separate exact
  mock/merge/lock-gated amendment;
- no Release, Deploy, Production Activation, Asset adoption or Export.

## 19. Review gate

D2 receives a new exact hash and independent DEV-4 review.  TASK-073-owned
Product source remains `START0` until D2 has unresolved `Critical=0`, `High=0`
and Judge `PASS`.  The exact successor mock independently requires
`Critical=0`, `High=0`, Judge `PASS` and explicit Owner check before Outcome E
may change TASK-036 UI/package source.  An Owner-check delay does not stop
TASK-073 composition/schema/fixture implementation.  Any further finding
creates D3 while D1/D2 identities remain in the review receipt.
