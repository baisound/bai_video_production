# TASK-051 R3 Tester Report

Pre-pack:
- exact perk/add-on slot mapping;
- Crop pixel equality with calibration editor;
- uncalibrated optional ROI fail-closed;
- R3 UI integration/static contracts;
- Python syntax.

Real worktree installer additionally runs R3, R2, R1 and affected TASK-050 regressions,
`py_compile`, and `git diff --check`.
