# v0.23.0 Editing SKILL Integration Release Evidence

Date: 2026-08-30
Task: TASK-058 release composition
Release identity: `0.23.0 / v0.23.0 / stable`
Authority source: Owner explicit request to integrate to main, create the Tag,
publish the Release, centralize Evidence in BVP main, and clean the completed
remote/local work branches.

## Scope

- TASK-058 installed exact3 read-only test seam and fail-closed child boundary;
- `scripts/probe_bvp_main.py` main-only, clean-checkout readiness probe;
- knowledge/commentary normalize -> transcribe -> cut candidates -> subtitle
  workspace -> Resolve handoff fixture integration;
- TASK-055/TASK-056 -> adapter -> receipt/Profile -> Resolve handoff fixture;
- shared privacy-safe, non-authoritative editing handoff projection;
- version, changelog, release metadata, hosted CI, Tag, Release, and cleanup.

## Local checkpoint

- base main: `d8c7ea58e2fb3d19fa65da69bb6bc14354506313`;
- pre-release implementation head: `21a561e9d2960029d3b9b8ab497cfca1069957a3`;
- implementation history: 35 linear commits, 9 paths, no merge commit;
- focused editing integration with actual read-only exact3: 70 PASS / 0 FAIL /
  0 SKIP;
- TASK-058 focused exact3 suite: 38 PASS / 0 FAIL / 0 SKIP;
- main probe WSL suite: 18 PASS / 0 FAIL;
- Windows targeted probe and index-immutability checks: PASS;
- full `src`/`tests` compileall on Windows: PASS;
- WSL full-suite collection: `NOT_CONFIRMED`; the pre-existing environment lacks
  `cryptography.hazmat.primitives.kdf.argon2.Argon2id` and `referencing`, so 28
  unrelated modules stopped during collection. No dependency install or upgrade
  was performed; the clean GitHub CI `.[dev]` environment is the required full
  regression gate;
- secret-like and private absolute-path additions: 0;
- worktree before release metadata: clean.

## Claim boundary

- The fixtures prove bounded static/local integration behavior only.
- Real DaVinci Resolve GUI runtime QA is `NOT_EXECUTED / NOT_CONFIRMED`.
- Endpoint pre/post equality does not prove continuous custody, ABA exclusion,
  writer exclusion, or restart-safe evidence.
- Connector activation, canonical Timeline mutation, Provider/paid execution,
  Deploy, and Production remain unauthorized and unclaimed.

## Hosted release read-back

- first PR CI attempt: six platform/Python jobs each reached the same canonical
  release-state assertion only; Ubuntu 3.12 reported 4708 PASS / 1 FAIL / 17
  SKIP. Cause: candidate docs set formal `Package` equal to `Development
  Candidate`; corrected by retaining formal Package 0.22.0 until release while
  keeping Development Candidate 0.23.0;
- release PR: `PENDING`;
- exact release-code main SHA: `PENDING`;
- hosted required checks: `PENDING`;
- annotated Tag object / dereferenced commit: `PENDING`;
- GitHub Release workflow: `PENDING`;
- published asset inventory and digests: `PENDING`;
- post-release Evidence PR/main read-back: `PENDING`;
- remote/local branch and worktree cleanup: `PENDING`.
